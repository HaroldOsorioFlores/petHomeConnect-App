import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  box_image: {
    width: 200,
    height: 200,
    borderColor: "black",
    borderWidth: 1
  },
});

export default styles;
